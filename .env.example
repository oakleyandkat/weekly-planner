# Copy this file to .env.local and fill in the values.
# For local dev, the default PGlite file works out of the box:
DATABASE_URL=pglite://./local.db

# For production (deploy to Suga or similar), set this to a real Postgres URL:
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
